<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.wolfcode.trip.base.mapper.SignMapper">
  <resultMap id="BaseResultMap" type="cn.wolfcode.trip.base.domain.Sign">
    <id column="id" property="id" />
   <!--<result column="user_id" property="userId" />-->
    <result column="add_time" property="addTime" />
    <result column="count" property="count" />
    <result column="score" property="score" />
    <association property="user" javaType="user" columnPrefix="u_">
      <id column="id" property="id" />
    </association>
  </resultMap>
  <delete id="deleteByPrimaryKey">
    delete from sign
    where id = #{id}
  </delete>
  <insert id="insert" keyProperty="id" useGeneratedKeys="true">
    insert into sign (user_id, add_time, score,count)
    values (#{user.id}, #{addTime}, #{score},#{count})
  </insert>
  <update id="updateByPrimaryKey">
    update sign
    set user_id = #{user.id},
      add_time = #{addTime},
      score = #{score},
      count = #{count}
    where id = #{id}
  </update>
  <select id="selectByPrimaryKey" resultMap="BaseResultMap">
    select id, user_id, add_time, score,count
    from sign
    where id = #{id}
  </select>
  <select id="selectAll" resultMap="BaseResultMap">
    select id, user_id, add_time, score,count
    from sign
  </select>
  <select id="selectByUserId" resultMap="BaseResultMap">
    select id, user_id, add_time, score,count
    from sign
    where  user_id = #{userId}
  </select>

</mapper>